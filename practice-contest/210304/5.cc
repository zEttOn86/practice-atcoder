#include <bits/stdc++.h>
#define rep(i, n) for(int i=0; i<(n); i++)
using namespace std;
using ll = long long;

int main()
{
    int r;
    cin >> r;
    double ans = 2 * M_PI * r;
    printf("%.10f\n", ans);
    return 0;
}