#include <bits/stdc++.h>
#define rep(i, n) for(int i=0; i<(n); i++)
using namespace std;
using ll = long long;

int main()
{
    int a, b, c;
    string s;
    cin >> a >> b >> c >> s;
    cout << a+b+c << " " << s << endl;
    
    return 0;
}