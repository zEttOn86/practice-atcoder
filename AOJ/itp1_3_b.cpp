#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;

int main()
{
    int x, i = 1;
    while(1)
    {
        cin >> x;
        if(x==0)
            break;
        cout << "Case " << i++ << ": " << x << endl;
    }

    return 0;
}